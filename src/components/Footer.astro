---
import XIcon from './XIcon.astro';
import InstagramIcon from './InstagramIcon.astro';
import GithubIcon from './GithubIcon.astro';
import NoteIcon from './NoteIcon.astro';
import HeaderSocialLink from './HeaderSocialLink.astro';
const today = new Date();

const socials = [
	{href: "https://github.com/SpinnerEddy", label: "SpinnerEddy on Github", Icon: GithubIcon},
	{href: "https://note.com/01122299", label: "SpinnerEddy on Note", Icon: NoteIcon},
];
---

<footer class="border-t bg-gray-50">
  <div class="mx-auto max-w-[1400px] px-4 py-10">
	<nav class="flex justify-center gap-6 mb-6">
		{socials.map(({href, label, Icon}) => (
			<HeaderSocialLink href={href} label={label}>
				<Icon class="w-6 h-6" />
			</HeaderSocialLink>
		))}
	</nav>
	<p class="text-center text-sm text-gray-500">
      © {today.getFullYear()} SpinnerEddy
    </p>
  </div>
</footer>